#!/bin/bash
# ============================================================================
# NixOS module — sourced by ../../setup.sh. Not run directly.
#
# NixOS is provisioned declaratively via configuration.nix, not by this
# imperative script. This is a placeholder until that flow is folded in (the
# eventual single-script goal). For now it just reports and exits cleanly.
# See linux/nixos/ and README.md.
# ============================================================================

section "Configuring for NixOS"
warn "Detected NixOS — manage the system via configuration.nix. See README.md."
log "No imperative provisioning to run here yet; skipping."
